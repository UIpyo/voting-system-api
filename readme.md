for auth = take metamask.address and password

only admin can post, put, delete electionData but anyone can get electionData

hasAccess -> Looks for the access feature

errorHandler -> Can create an errorHandler